export const useUtilities = () => {
    // const saveState = (key: string, value: any) => {
    //     const cookie = useCookie(key)
    //     cookie.value = cookie.value || value
    //     return useState(key, () => cookie);
    // }


    // return { saveState }
}